import React from 'react';
import css from './SocNetHead.module.css';

const SocNetHead = (props) => { return <a href=""><img src={props.icon} alt="socNetwork" /></a> }

export default SocNetHead;